[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "qunet2"
version = "1.0.0"
description = "QUNet 2.0 retinal analysis repository"

[tool.pytest.ini_options]
testpaths = ["tests"]
